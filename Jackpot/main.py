import sys

mots = []

for line in sys.stdin:
    words = line.strip().split()
    for word in words:
        if word == "0":
            break
        mots.append(int(word))

enCours = 0
maxe = nb[0]
for nb in mots:
    if enCours = maxe:
        maxe = nb[maxe]
    else:
        liste
